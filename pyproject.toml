[project]
name = "runstat"
version = "0.1.0"
description = "Post-hoc telemetry analysis for autonomous loop runs."
requires-python = ">=3.13"
dependencies = []

[project.scripts]
runstat = "runstat.__main__:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/runstat"]
