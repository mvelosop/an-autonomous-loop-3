{
  "task": "T4",
  "verdict": "PASS",
  "criteria": [
    {"criterion": ".loop/amend.sh check prints an advisory naming each task whose verify diffs against HEAD (git diff ... HEAD ..., git diff --quiet HEAD) and mentions neither LOOP_ACTIVE_TASK nor LOOP_GATE_TASK, and explains that during another task's iteration the only uncommitted work is that task's.", "met": true, "evidence": ".loop/amend.sh lines 117-144: jq regex over .verify now matches git diff|log|rev-list ... HEAD, excludes tasks mentioning either variable, and the say lines explain the reasoning."},
    {"criterion": "It is advisory only: check still exits 0 when this is the only finding, exactly as the two existing advisories do, and a task whose verify mentions either variable, or does not diff against HEAD at all, is not named.", "met": true, "evidence": "The block only calls say, never sets problems=1; scenario 38's quiet cases assert exit 0 and no T2 mention."},
    {"criterion": "The existing structural checks and the two existing advisories in check() behave as before.", "met": true, "evidence": "Diff is confined to the comment and the regex line; shared-file and passing-gate advisories untouched; full 41-scenario suite passes."},
    {"criterion": "A new scenario .loop/tests/scenarios/38-head-diff-advisory.sh asserts the advised and the not-advised cases and the exit code.", "met": true, "evidence": "File present (added in the prior review_fail commit, unchanged here); ran standalone and inside run-all.sh, all 12 assertions pass."},
    {"criterion": ".claude/skills/loop-plan/SKILL.md gains the paragraph for this advisory beside rules 3 and 4, saying how a HEAD-diff scope guard should use the two variables.", "met": true, "evidence": "SKILL.md lines 89/95/119 describe the advisory as covering git diff/log/rev-list against HEAD; amend.sh's regex now actually matches all three, closing the prior review's sole finding (doc claimed log/rev-list coverage the code didn't have)."},
    {"criterion": "The new scenario file is added under .loop/tests/scenarios/ ... and every document check-docs.sh holds to the suite size ... is updated in the same change so check-docs.sh stays green; check-docs.sh itself is not weakened, exempted or edited.", "met": true, "evidence": "check-docs passes in the full run; README.md, .loop/README.md, .loop/manual.md, B008-driver-in-python.md and the run's own brief carry the 40->41/37->38 bumps from the prior commit, untouched by this iteration's diff."},
    {"criterion": "The whole offline suite (.loop/tests/run-all.sh) and uv run pytest -q stay green: existing scenarios keep asserting what they asserted before, and none is deleted or loosened to make room for this change.", "met": true, "evidence": "Ran both directly: 41 passed (run-all.sh), 40 passed in 1.02s (pytest)."}
  ],
  "findings": [],
  "notes": "This iteration's diff is a single-line regex fix (git[[:space:]]+diff -> git[[:space:]]+(diff|log|rev-list)) plus a comment update in .loop/amend.sh, exactly closing the one finding from the prior FAIL verdict (doc claimed log/rev-list coverage the regex didn't have). Verified the fix against the actual proposal.json claim rather than trusting it: reran scenario 38, the full offline suite, and pytest myself, all green. Nothing else in the tree changed since the review_fail commit."
}
